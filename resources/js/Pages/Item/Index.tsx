import { PageSection } from '@/Components/PageSection';
import { PageProps } from '@/types';

const Index = (props: PageProps) => {
  return <PageSection {...props}>item</PageSection>;
};

export default Index;
